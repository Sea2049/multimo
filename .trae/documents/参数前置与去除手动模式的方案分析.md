# 方案三：混合方案 - 实施计划

## 🎯 目标
将手动模式改为可选的高级功能，默认使用自动驾驶模式，同时保留高级用户的精细控制能力。

## 📝 具体实施步骤

### Phase 1: Home.vue UI 改造

#### 1.1 重构模式选择区域
**当前结构：**
```
03 / 运行模式
  ├─ 自动驾驶 (默认选中)
  └─ 手动模式
```

**改造后结构：**
```
03 / 模拟轮数 (提前)
  ├─ 输入框 (默认 15)
  └─ 建议范围提示

[高级选项] (可折叠)
  └─ ☐ 启用手动模式
      └─ 说明：每个步骤需要手动确认，适合调试和精细控制
```

#### 1.2 调整参数顺序
- 将"模拟轮数"从 04 提升到 03
- 将"运行模式"移入高级选项
- 默认 mode = 'auto'，只有勾选才变为 'manual'

#### 1.3 添加智能提示
- 根据文件数量和大小给出轮数建议
- 显示预估运行时间
- 添加"为什么需要手动模式"的说明

### Phase 2: 代码修改清单

#### 文件 1: `frontend/src/views/Home.vue`

**修改点：**
1. 删除原有的模式选择器 UI (第 189-221 行)
2. 将轮数输入提前到 03 位置
3. 添加高级选项折叠面板
4. 添加手动模式 checkbox
5. 添加智能推荐逻辑
6. 优化 CSS 样式

**新增状态：**
```javascript
const showAdvanced = ref(false) // 是否展开高级选项
const enableManualMode = ref(false) // 是否启用手动模式
```

**计算属性：**
```javascript
const estimatedTime = computed(() => {
  // 根据文件大小和轮数预估时间
  const baseTime = formData.value.rounds * 0.6 // 每轮约0.6分钟
  return Math.round(baseTime)
})

const recommendedRounds = computed(() => {
  // 根据文件数量推荐轮数
  if (files.value.length === 0) return 15
  if (files.value.length <= 2) return 10
  if (files.value.length <= 5) return 15
  return 20
})
```

#### 文件 2: `frontend/src/store/pendingUpload.js`

**无需修改** - 保持现有的 mode 参数传递

#### 文件 3: `frontend/src/views/MainView.vue`

**无需修改** - 保持现有的自动/手动逻辑

#### 文件 4: `frontend/src/components/Step2EnvSetup.vue`

**无需修改** - 保持现有的自动启动逻辑

### Phase 3: UI 设计细节

#### 高级选项面板样式
```css
.advanced-options {
  margin-top: 16px;
  border: 1px solid #EAEAEA;
  border-radius: 8px;
  overflow: hidden;
}

.advanced-header {
  padding: 12px 16px;
  background: #F5F5F5;
  cursor: pointer;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.advanced-content {
  padding: 16px;
  background: #FAFAFA;
}

.manual-mode-checkbox {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.mode-explanation {
  font-size: 13px;
  color: #666;
  line-height: 1.6;
  margin-top: 8px;
}
```

### Phase 4: 文案优化

#### 主界面文案
- **03 / 模拟轮数**: "设置模拟推演的轮次，每轮代表现实时间流逝"
- **建议**: "根据您上传的 X 个文件，建议设置 Y 轮"
- **预估**: "预计运行时间约 Z 分钟（基于100个Agent）"

#### 高级选项文案
- **标题**: "⚙️ 高级选项"
- **手动模式说明**: 
  ```
  启用手动模式后，每个步骤将暂停等待您的确认。
  适用场景：
  • 需要查看中间结果
  • 调整模拟参数
  • 开发调试
  
  大多数情况下，推荐使用默认的自动驾驶模式。
  ```

## 🎨 视觉效果

### 默认状态（90%用户看到的）
```
┌─────────────────────────────────┐
│ 02 / 模拟提示词                  │
│ [文本输入框]                     │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ 03 / 模拟轮数                    │
│ [15] Rounds                      │
│ 💡 建议: 10-20轮                 │
│ ⏱️ 预计: 约9分钟                 │
└─────────────────────────────────┘

┌─────────────────────────────────┐
│ ⚙️ 高级选项 ▼                   │
└─────────────────────────────────┘

[启动引擎 →]
```

### 展开高级选项后（10%高级用户）
```
┌─────────────────────────────────┐
│ ⚙️ 高级选项 ▲                   │
├─────────────────────────────────┤
│ ☐ 启用手动模式                   │
│                                  │
│ 启用后每个步骤将暂停等待确认。    │
│ 适合调试和精细控制。              │
│                                  │
│ 大多数情况推荐使用自动驾驶。      │
└─────────────────────────────────┘
```

## ✅ 验收标准

1. **默认行为**
   - 不展开高级选项时，mode = 'auto'
   - 流程自动执行，无需人工干预
   - 轮数设置清晰可见

2. **高级功能**
   - 展开高级选项后可勾选手动模式
   - 勾选后 mode = 'manual'
   - 手动模式下每步需要确认

3. **用户体验**
   - 新手用户：简单直接，一键完成
   - 高级用户：可选择精细控制
   - 文案清晰，不产生困惑

4. **代码质量**
   - 保持现有逻辑不变
   - 只修改 UI 层
   - 向后兼容

## 📊 预期效果

- **90%用户**: 看到简化界面，快速启动
- **10%用户**: 展开高级选项，精细控制
- **开发者**: 保留调试能力
- **代码**: 主流程更清晰，维护成本降低