# ===========================================
# Multimo 生产环境配置模板
# 复制此文件为 .env 并填入实际值
# ===========================================

# --- Flask 配置 ---
# 生产环境必须修改此密钥
SECRET_KEY=change-this-to-a-secure-random-string
# 生产环境必须设为 False
DEBUG=False
HOST=0.0.0.0
PORT=5001

# --- LLM 配置 (必须) ---
# 您的 OpenAI API Key 或兼容服务的 Key
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# LLM 服务地址 (例如 OpenAI 或 Azure)
LLM_BASE_URL=https://api.openai.com/v1
# 使用的模型名称
LLM_MODEL_NAME=gpt-4o-mini
# 温度 (0.0 - 1.0)
LLM_TEMPERATURE=0.7

# --- Zep Cloud 配置 (可选) ---
# 如果使用 Zep 记忆服务，请填写
ZEP_API_KEY=

# --- CORS 配置 ---
# 允许访问的域名，生产环境请修改为您的实际域名
# 格式: JSON 数组字符串
CORS_ORIGINS=["https://your-domain.com", "https://www.your-domain.com"]

# --- 安全配置 ---
# 是否启用 API Key 认证
API_KEY_ENABLED=True
# API Key 列表 (JSON 格式)
API_KEYS=[{"id": "admin", "key": "your-secure-api-key", "name": "Admin User"}]

# --- 路径配置 (通常不需要修改，Docker 内已配置) ---
# UPLOAD_FOLDER=/app/uploads
# SIMULATION_DATA_DIR=/app/uploads/simulations
# LOG_DIR=/app/logs
