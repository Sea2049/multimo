# 断点续传功能使用示例

## 📚 目录
1. [基础示例](#基础示例)
2. [完整工作流](#完整工作流)
3. [错误处理](#错误处理)
4. [监控和调试](#监控和调试)
5. [生产环境部署](#生产环境部署)

---

## 基础示例

### 示例1: 最简单的使用方式

```bash
#!/bin/bash

# 设置变量
SIMULATION_ID="sim_123456"
API_URL="http://localhost:5001/api/simulation"

# 1. 配置自动驾驶模式
echo "1. 配置自动驾驶模式..."
curl -X POST "$API_URL/auto-pilot/config" \
  -H "Content-Type: application/json" \
  -d "{
    \"simulation_id\": \"$SIMULATION_ID\",
    \"mode\": \"auto\"
  }"

echo ""
echo "2. 启动自动驾驶（支持断点续传）..."
curl -X POST "$API_URL/auto-pilot/start" \
  -H "Content-Type: application/json" \
  -d "{
    \"simulation_id\": \"$SIMULATION_ID\",
    \"force\": false
  }"

echo ""
echo "3. 查询状态..."
curl -X POST "$API_URL/auto-pilot/status" \
  -H "Content-Type: application/json" \
  -d "{
    \"simulation_id\": \"$SIMULATION_ID\"
  }"
```

### 示例2: Python 脚本

```python
import requests
import json
import time

# 配置
SIMULATION_ID = "sim_123456"
API_URL = "http://localhost:5001/api/simulation"

def config_auto_pilot():
    """配置自动驾驶"""
    url = f"{API_URL}/auto-pilot/config"
    payload = {
        "simulation_id": SIMULATION_ID,
        "mode": "auto"
    }
    response = requests.post(url, json=payload)
    return response.json()

def start_auto_pilot(force=False):
    """启动自动驾驶"""
    url = f"{API_URL}/auto-pilot/start"
    payload = {
        "simulation_id": SIMULATION_ID,
        "force": force
    }
    response = requests.post(url, json=payload)
    return response.json()

def get_status():
    """获取状态"""
    url = f"{API_URL}/auto-pilot/status"
    payload = {
        "simulation_id": SIMULATION_ID
    }
    response = requests.post(url, json=payload)
    return response.json()

# 执行
print("1. 配置自动驾驶...")
result = config_auto_pilot()
print(json.dumps(result, indent=2, ensure_ascii=False))

print("\n2. 启动自动驾驶...")
result = start_auto_pilot(force=False)
print(json.dumps(result, indent=2, ensure_ascii=False))

print("\n3. 查询状态...")
result = get_status()
print(json.dumps(result, indent=2, ensure_ascii=False))
```

---

## 完整工作流

### 场景1: 正常执行流程

```python
import requests
import json
import time

class SimulationController:
    def __init__(self, simulation_id, api_url="http://localhost:5001/api/simulation"):
        self.simulation_id = simulation_id
        self.api_url = api_url
    
    def config(self, mode="auto"):
        """配置模式"""
        url = f"{self.api_url}/auto-pilot/config"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id,
            "mode": mode
        })
        return response.json()
    
    def start(self, force=False):
        """启动"""
        url = f"{self.api_url}/auto-pilot/start"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id,
            "force": force
        })
        return response.json()
    
    def status(self):
        """获取状态"""
        url = f"{self.api_url}/auto-pilot/status"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def pause(self):
        """暂停"""
        url = f"{self.api_url}/auto-pilot/pause"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def resume(self):
        """恢复"""
        url = f"{self.api_url}/auto-pilot/resume"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def stop(self):
        """停止"""
        url = f"{self.api_url}/auto-pilot/stop"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def reset(self):
        """重置"""
        url = f"{self.api_url}/auto-pilot/reset"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def run_until_complete(self, check_interval=30, max_wait=72*3600):
        """运行直到完成"""
        print("启动自动驾驶...")
        self.config(mode="auto")
        self.start(force=False)
        
        elapsed = 0
        while elapsed < max_wait:
            time.sleep(check_interval)
            elapsed += check_interval
            
            status_resp = self.status()
            if not status_resp.get("success"):
                print(f"❌ 获取状态失败: {status_resp}")
                break
            
            data = status_resp.get("data", {})
            status = data.get("status")
            current_step = data.get("current_step")
            progress = data.get("step_progress", 0)
            message = data.get("step_message", "")
            
            print(f"[{elapsed//60}分钟] 状态: {status}, 步骤: {current_step}, 进度: {progress}%, 消息: {message}")
            
            if status == "completed":
                print("✅ 模拟完成！")
                return True
            elif status == "failed":
                error = data.get("error", "未知错误")
                print(f"❌ 模拟失败: {error}")
                return False
        
        print("⏱️ 超时（超过72小时）")
        return False

# 使用
controller = SimulationController("sim_123456")
controller.run_until_complete()
```

### 场景2: 支持暂停和恢复

```python
import requests
import json
import time

class SimulationControllerWithPause:
    def __init__(self, simulation_id, api_url="http://localhost:5001/api/simulation"):
        self.simulation_id = simulation_id
        self.api_url = api_url
    
    def config(self, mode="auto"):
        url = f"{self.api_url}/auto-pilot/config"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id,
            "mode": mode
        })
        return response.json()
    
    def start(self, force=False):
        url = f"{self.api_url}/auto-pilot/start"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id,
            "force": force
        })
        return response.json()
    
    def status(self):
        url = f"{self.api_url}/auto-pilot/status"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def pause(self):
        url = f"{self.api_url}/auto-pilot/pause"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def resume(self):
        url = f"{self.api_url}/auto-pilot/resume"
        response = requests.post(url, json={
            "simulation_id": self.simulation_id
        })
        return response.json()
    
    def run_with_pause_support(self, check_interval=30, pause_after=300):
        """运行并支持暂停"""
        print("启动自动驾驶...")
        self.config(mode="auto")
        self.start(force=False)
        
        elapsed = 0
        paused = False
        
        while True:
            time.sleep(check_interval)
            elapsed += check_interval
            
            status_resp = self.status()
            if not status_resp.get("success"):
                print(f"❌ 获取状态失败")
                break
            
            data = status_resp.get("data", {})
            status = data.get("status")
            progress = data.get("step_progress", 0)
            message = data.get("step_message", "")
            
            print(f"[{elapsed}秒] 状态: {status}, 进度: {progress}%, 消息: {message}")
            
            # 在指定时间后暂停
            if elapsed == pause_after and not paused:
                print(f"\n⏸️  在 {pause_after} 秒时暂停...")
                self.pause()
                paused = True
                
                # 暂停 30 秒
                print("暂停 30 秒...")
                time.sleep(30)
                
                # 恢复
                print("🔄 恢复执行...")
                self.resume()
            
            if status == "completed":
                print("✅ 模拟完成！")
                return True
            elif status == "failed":
                error = data.get("error", "未知错误")
                print(f"❌ 模拟失败: {error}")
                return False

# 使用
controller = SimulationControllerWithPause("sim_123456")
controller.run_with_pause_support(pause_after=300)  # 5分钟后暂停
```

---

## 错误处理

### 示例3: 完善的错误处理

```python
import requests
import json
import time
from typing import Optional, Dict, Any

class RobustSimulationController:
    def __init__(self, simulation_id, api_url="http://localhost:5001/api/simulation"):
        self.simulation_id = simulation_id
        self.api_url = api_url
        self.max_retries = 3
    
    def _request(self, method: str, endpoint: str, payload: Dict) -> Optional[Dict]:
        """发送请求，带重试机制"""
        url = f"{self.api_url}/{endpoint}"
        
        for attempt in range(self.max_retries):
            try:
                response = requests.post(url, json=payload, timeout=10)
                return response.json()
            except requests.exceptions.Timeout:
                print(f"⚠️  请求超时 (尝试 {attempt+1}/{self.max_retries})")
                if attempt < self.max_retries - 1:
                    time.sleep(2 ** attempt)  # 指数退避
            except requests.exceptions.ConnectionError:
                print(f"⚠️  连接失败 (尝试 {attempt+1}/{self.max_retries})")
                if attempt < self.max_retries - 1:
                    time.sleep(2 ** attempt)
            except Exception as e:
                print(f"❌ 请求失败: {e}")
                return None
        
        return None
    
    def config(self, mode="auto") -> bool:
        """配置模式"""
        result = self._request("POST", "auto-pilot/config", {
            "simulation_id": self.simulation_id,
            "mode": mode
        })
        
        if result and result.get("success"):
            print(f"✅ 配置成功: mode={mode}")
            return True
        else:
            error = result.get("error", "未知错误") if result else "请求失败"
            print(f"❌ 配置失败: {error}")
            return False
    
    def start(self, force=False) -> bool:
        """启动"""
        result = self._request("POST", "auto-pilot/start", {
            "simulation_id": self.simulation_id,
            "force": force
        })
        
        if result and result.get("success"):
            print(f"✅ 启动成功 (force={force})")
            return True
        else:
            error = result.get("error", "未知错误") if result else "请求失败"
            print(f"❌ 启动失败: {error}")
            return False
    
    def status(self) -> Optional[Dict]:
        """获取状态"""
        result = self._request("POST", "auto-pilot/status", {
            "simulation_id": self.simulation_id
        })
        
        if result and result.get("success"):
            return result.get("data")
        else:
            error = result.get("error", "未知错误") if result else "请求失败"
            print(f"❌ 获取状态失败: {error}")
            return None
    
    def reset(self) -> bool:
        """重置"""
        result = self._request("POST", "auto-pilot/reset", {
            "simulation_id": self.simulation_id
        })
        
        if result and result.get("success"):
            print("✅ 重置成功")
            return True
        else:
            error = result.get("error", "未知错误") if result else "请求失败"
            print(f"❌ 重置失败: {error}")
            return False
    
    def run_with_error_handling(self):
        """运行，带完善的错误处理"""
        print("=" * 60)
        print("启动模拟（带错误处理）")
        print("=" * 60)
        
        # 步骤1: 配置
        if not self.config(mode="auto"):
            print("❌ 配置失败，退出")
            return False
        
        # 步骤2: 启动
        if not self.start(force=False):
            print("❌ 启动失败，尝试重置后重新启动...")
            if self.reset():
                if not self.start(force=True):
                    print("❌ 重新启动失败，退出")
                    return False
            else:
                print("❌ 重置失败，退出")
                return False
        
        # 步骤3: 监控
        elapsed = 0
        check_interval = 30
        max_wait = 72 * 3600
        
        while elapsed < max_wait:
            time.sleep(check_interval)
            elapsed += check_interval
            
            status_data = self.status()
            if not status_data:
                print("⚠️  无法获取状态，继续等待...")
                continue
            
            status = status_data.get("status")
            progress = status_data.get("step_progress", 0)
            message = status_data.get("step_message", "")
            error = status_data.get("error")
            
            print(f"[{elapsed//60}分钟] 状态: {status}, 进度: {progress}%, 消息: {message}")
            
            if error:
                print(f"⚠️  错误: {error}")
            
            if status == "completed":
                print("✅ 模拟完成！")
                return True
            elif status == "failed":
                print(f"❌ 模拟失败: {error}")
                print("尝试重置并重新启动...")
                if self.reset() and self.start(force=True):
                    print("✅ 已重新启动")
                else:
                    print("❌ 重新启动失败")
                    return False
        
        print("⏱️ 超时")
        return False

# 使用
controller = RobustSimulationController("sim_123456")
controller.run_with_error_handling()
```

---

## 监控和调试

### 示例4: 实时监控

```bash
#!/bin/bash

# 实时监控脚本
SIMULATION_ID="sim_123456"
API_URL="http://localhost:5001/api/simulation"

echo "实时监控模拟进度..."
echo "按 Ctrl+C 停止"
echo ""

while true; do
    # 获取状态
    response=$(curl -s -X POST "$API_URL/auto-pilot/status" \
      -H "Content-Type: application/json" \
      -d "{\"simulation_id\": \"$SIMULATION_ID\"}")
    
    # 解析 JSON
    status=$(echo "$response" | jq -r '.data.status')
    current_step=$(echo "$response" | jq -r '.data.current_step')
    progress=$(echo "$response" | jq -r '.data.step_progress')
    message=$(echo "$response" | jq -r '.data.step_message')
    
    # 打印
    timestamp=$(date '+%Y-%m-%d %H:%M:%S')
    echo "[$timestamp] 状态: $status | 步骤: $current_step | 进度: $progress% | $message"
    
    # 等待 30 秒
    sleep 30
done
```

### 示例5: 查看状态文件

```bash
#!/bin/bash

# 查看状态文件
SIMULATION_ID="sim_123456"
STATE_FILE="backend/data/simulations/auto_pilot/${SIMULATION_ID}_autopilot.json"

echo "状态文件: $STATE_FILE"
echo ""

if [ -f "$STATE_FILE" ]; then
    echo "内容:"
    cat "$STATE_FILE" | jq '.'
else
    echo "❌ 状态文件不存在"
fi
```

---

## 生产环境部署

### 示例6: 生产环境脚本

```python
#!/usr/bin/env python3
"""
生产环境自动驾驶管理脚本
"""

import requests
import json
import time
import logging
from datetime import datetime
from typing import Optional, Dict, Any

# 配置日志
logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('auto_pilot.log'),
        logging.StreamHandler()
    ]
)
logger = logging.getLogger(__name__)

class ProductionSimulationController:
    """生产环境模拟控制器"""
    
    def __init__(self, simulation_id: str, api_url: str = "http://localhost:5001/api/simulation"):
        self.simulation_id = simulation_id
        self.api_url = api_url
        self.max_retries = 5
        self.retry_delay = 5
    
    def _request(self, endpoint: str, payload: Dict) -> Optional[Dict]:
        """发送请求，带重试机制"""
        url = f"{self.api_url}/{endpoint}"
        
        for attempt in range(self.max_retries):
            try:
                response = requests.post(url, json=payload, timeout=30)
                response.raise_for_status()
                return response.json()
            except requests.exceptions.RequestException as e:
                logger.warning(f"请求失败 (尝试 {attempt+1}/{self.max_retries}): {e}")
                if attempt < self.max_retries - 1:
                    time.sleep(self.retry_delay * (2 ** attempt))
        
        logger.error(f"请求最终失败: {endpoint}")
        return None
    
    def config(self, mode: str = "auto") -> bool:
        """配置模式"""
        result = self._request("auto-pilot/config", {
            "simulation_id": self.simulation_id,
            "mode": mode
        })
        
        if result and result.get("success"):
            logger.info(f"配置成功: mode={mode}")
            return True
        else:
            logger.error(f"配置失败: {result}")
            return False
    
    def start(self, force: bool = False) -> bool:
        """启动"""
        result = self._request("auto-pilot/start", {
            "simulation_id": self.simulation_id,
            "force": force
        })
        
        if result and result.get("success"):
            logger.info(f"启动成功 (force={force})")
            return True
        else:
            logger.error(f"启动失败: {result}")
            return False
    
    def status(self) -> Optional[Dict]:
        """获取状态"""
        result = self._request("auto-pilot/status", {
            "simulation_id": self.simulation_id
        })
        
        if result and result.get("success"):
            return result.get("data")
        else:
            logger.error(f"获取状态失败: {result}")
            return None
    
    def run(self):
        """运行模拟"""
        logger.info(f"开始运行模拟: {self.simulation_id}")
        
        # 配置
        if not self.config(mode="auto"):
            logger.error("配置失败")
            return False
        
        # 启动
        if not self.start(force=False):
            logger.error("启动失败")
            return False
        
        # 监控
        check_interval = 60  # 每分钟检查一次
        max_wait = 72 * 3600  # 最多等待 72 小时
        elapsed = 0
        
        while elapsed < max_wait:
            time.sleep(check_interval)
            elapsed += check_interval
            
            status_data = self.status()
            if not status_data:
                logger.warning("无法获取状态")
                continue
            
            status = status_data.get("status")
            progress = status_data.get("step_progress", 0)
            message = status_data.get("step_message", "")
            
            logger.info(f"进度: {status} | {progress}% | {message}")
            
            if status == "completed":
                logger.info("✅ 模拟完成")
                return True
            elif status == "failed":
                error = status_data.get("error", "未知错误")
                logger.error(f"❌ 模拟失败: {error}")
                return False
        
        logger.error("⏱️ 超时（超过72小时）")
        return False

if __name__ == "__main__":
    import sys
    
    if len(sys.argv) < 2:
        print("用法: python production_controller.py <simulation_id>")
        sys.exit(1)
    
    simulation_id = sys.argv[1]
    controller = ProductionSimulationController(simulation_id)
    
    success = controller.run()
    sys.exit(0 if success else 1)
```

---

## 总结

这些示例展示了如何在不同场景下使用断点续传功能：

1. **基础示例**: 最简单的使用方式
2. **完整工作流**: 从启动到完成的完整流程
3. **错误处理**: 如何处理各种错误情况
4. **监控和调试**: 如何监控和调试
5. **生产环境**: 生产环境的最佳实践

所有示例都支持断点续传功能，即使服务重启也能自动从断点继续执行。

