# ===========================================
# Multimo Staging 环境配置模板
# 复制此文件为 .env.staging 并填入实际值
# ===========================================

# --- Flask 配置 ---
# Staging 环境可以开启 Debug
SECRET_KEY=change-this-to-a-secure-random-string-staging
DEBUG=True
HOST=0.0.0.0
PORT=5002

# --- LLM 配置 (必须) ---
# 您的 OpenAI API Key 或兼容服务的 Key
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# LLM 服务地址 (例如 OpenAI 或 Azure)
LLM_BASE_URL=https://api.openai.com/v1
# 使用的模型名称
LLM_MODEL_NAME=gpt-4o-mini
# 温度 (0.0 - 1.0)
LLM_TEMPERATURE=0.7

# --- Zep Cloud 配置 (可选) ---
# 如果使用 Zep 记忆服务，请填写
ZEP_API_KEY=

# --- CORS 配置 ---
# Staging 环境域名
CORS_ORIGINS=["https://test.sea-ming.com"]

# --- 安全配置 ---
# Staging 环境可以使用不同的 API Key
API_KEY_ENABLED=True
API_KEYS=[{"id": "staging-admin", "key": "your-staging-api-key", "name": "Staging Admin"}]

# --- 路径配置 (通常不需要修改，Docker 内已配置) ---
# UPLOAD_FOLDER=/app/uploads
# SIMULATION_DATA_DIR=/app/uploads/simulations
# LOG_DIR=/app/logs
